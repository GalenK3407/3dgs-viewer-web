# 3DGS Viewer

一个基于 `Spark.js` 和 `Three.js` 的 3D Gaussian Splatting 网页查看器，当前版本聚焦于两条主线：

- 固定中心点的镜头规划与 MP4 导出
- 浏览器内的 splat 删除编辑与 `.ply` 保存

这版已经不是“手动拖相机再记关键帧”的旧流程，而是 `v1.0` 的镜头点规划版本：

- 双击模型设置固定镜头中心 `Pivot`
- 用离散镜头点规划路径
- 所有镜头点和中间采样帧始终朝向 `Pivot`
- `P` 预览和导出视频走同一套路径采样逻辑

## 当前能力

- 本地选择或拖拽上传 3DGS 场景并自动对焦
- 运行时将场景主平面对齐到固定世界坐标系，减少旋转语义混乱
- 双击模型设置固定镜头中心
- 镜头规划面板：插点、删点、清空、预览
- 镜头点编号、朝向箭头、连线、路径线可视化
- 预览与导出共用同一条镜头路径
- Picker / Brush 两套 splat 删除编辑方式
- 撤销 / 重做 / 保存当前可见 splats 为 `.ply`

## 界面截图

仓库当前没有提交截图资源，所以 README 里还不能直接放“真实界面图”。

如果你要做图文版 README，建议把截图放到 `docs/images/`，例如：

- `docs/images/overview.png`
- `docs/images/shot-planner.png`
- `docs/images/edit-mode.png`
- `docs/images/export-panel.png`

我已经按当前代码把 README 内容整理好了。你把截图文件补进仓库后，我可以继续把它们插进对应章节。

## 快速开始

### 环境要求

- Chrome / Edge，且浏览器支持 `WebCodecs`
- 通过 HTTP 服务器访问，不能直接双击 `index.html`

### 文件准备

目录示例：

```text
3dgs-viewer-web/
├── index.html
├── viewer.js
├── README.md
└── LICENSE
```

### 启动方式

```bash
python -m http.server 8080
```

然后访问：

```text
http://localhost:8080
```

## 推荐使用流程

### 1. 上传模型

- 页面初始进入是空态，不会自动加载仓库中的固定模型
- 点击左侧 `打开文件` 或中心空态按钮，选择本地 3DGS 文件
- 也可以直接把模型文件拖到页面中
- 支持格式：`.ply` / `.splat` / `.spz` / `.ksplat`

### 2. 浏览场景

- 左键拖拽旋转视角
- 右键拖拽平移
- 滚轮缩放
- 方向键做视角转向

### 3. 设置镜头中心

- 双击模型，设置固定镜头中心 `Pivot`
- 设置后，镜头规划、路径预览、视频导出都围绕这个中心工作
- 普通平移和缩放不会再改动 `Pivot`

### 4. 进入镜头规划

- 点击左上角 `进入规划`
- 初始不会自动显示默认镜头点
- 进入规划后，按 `+` 会从当前相机机位插入一个镜头点

### 5. 组织镜头路径

- 点击镜头点可切到该点视角预览
- `+`：在当前选中点后插入新点；未选中时追加到末尾
- `Del`：删除当前镜头点
- `C`：清空所有镜头点
- `P`：按镜头点顺序预览整条路径

注意：

- 至少需要 `2` 个镜头点，才能播放和导出
- 所有镜头点默认始终朝向中心点
- 两个镜头点之间的中间采样帧也会持续 `lookAt(Pivot)`

### 6. 导出视频

- 右上角设置分辨率、FPS、总时长、码率
- 点击 `导出 MP4 视频`
- 导出使用离屏渲染路径，和预览共享同一套路径采样逻辑

## 交互说明

### 导航

| 操作 | 说明 |
| --- | --- |
| 点击 `打开文件` | 选择本地 3DGS 模型 |
| 拖拽文件到页面 | 加载本地 3DGS 模型 |
| 左键拖拽 | 自由旋转视角 |
| 右键拖拽 | 平移 |
| 滚轮 | 缩放 |
| 方向键 | 旋转 / 俯仰视角 |
| `R` + 左键拖拽 | 围绕当前中心做环绕观察 |
| 双击模型 | 设置或重设固定镜头中心 |

### 镜头规划

| 操作 | 说明 |
| --- | --- |
| `进入规划` | 切到镜头规划模式 |
| 点击镜头点 | 选中并预览该镜头点 |
| `+` | 从当前机位插入镜头点 |
| `Del` | 删除当前镜头点 |
| `C` | 清空所有镜头点 |
| `P` | 播放 / 停止路径预览 |

### 删除编辑

| 操作 | 说明 |
| --- | --- |
| `E` | 进入 / 退出删除编辑模式 |
| `1` | Picker |
| `2` | Brush |
| Picker 左键点击 | 单点选择 |
| Picker 左键拖拽 | 框选 |
| Brush 左键拖拽 | 刷选 |
| `Shift` | 加选 |
| `Ctrl/Cmd` | 减选 |
| `[` / `]` | 调整画笔半径 |
| `Esc` | 清空选择 |
| `Del` | 删除选中的 splats |
| `Ctrl+Z` | 撤销删除 |
| `Ctrl+Y` | 重做删除 |
| `Ctrl+S` | 导出当前可见 splats 为 `.ply` |

删除键语义和模式有关：

- 镜头规划模式下，`Del` 删除镜头点
- 删除编辑模式下，`Del` 删除 splats

## 功能结构

### 1. 固定中心点镜头规划

当前镜头系统基于：

- `shotPivot`：固定中心点
- `shotPoints`：镜头点数组

每个镜头点存的是参数化位置，而不是手调四元数：

- `radius`
- `azimuth`
- `height`

真实相机位置由 `shotPivot + 极坐标参数` 实时计算，相机朝向统一通过 `lookAt(shotPivot)` 生成。

### 2. 预览与导出一致

`P` 预览和 MP4 导出都消费同一套由镜头点派生出的路径，因此：

- 路径顺序一致
- 采样方向一致
- 预览和导出画幅逻辑一致

导出时会自动隐藏编辑辅助元素，避免把操作标记带进视频。

### 3. 删除编辑与保存

删除编辑不修改原始源文件，而是在运行时隐藏对应 splats。保存时会把当前可见内容重新写出为一个新的 `.ply` 文件。

适合用来：

- 去掉漂浮噪点
- 裁掉场景边缘杂点
- 保存一个更干净的展示版本

## 模型路径与格式

当前版本不再依赖固定的 `./model.ply` 初始模型，而是由用户在页面中主动选择或拖拽本地文件。

当前上传入口支持以下常见 3DGS 格式：

- `.ply`
- `.splat`
- `.spz`
- `.ksplat`

模型切换成功后，会自动重置当前镜头规划和删除编辑状态，并重新对焦到新模型。

## 仓库结构

```text
3dgs-viewer-web/
├── index.html
├── viewer.js
├── README.md
└── LICENSE
```

## 已知限制

- 当前是纯静态前端页面，没有后端
- 当前只支持本地上传，不支持服务端存储或 URL 载入
- README 还没有配套真实截图资源
- 目前没有镜头点数值编辑器，镜头点主要通过当前机位插入

## 二次开发建议

如果你准备继续扩展，可以优先做这些方向：

- 给镜头点增加显式列表和重排能力
- 给本地上传补更明确的加载进度和错误提示
- 给 README / 演示页补截图和示例视频
- 加一个构建流程，例如 `Vite`
- 将 `viewer.js` 拆成导航、镜头规划、编辑导出等模块

## 致谢

- [Spark.js](https://github.com/sparkjsdev/spark)
- [Three.js](https://github.com/mrdoob/three.js)
- [mp4-muxer](https://github.com/Vanilagy/mp4-muxer)

## License

本仓库采用 [MIT License](./LICENSE)。
